// ═══════════════════════════════════════════════════════
//  REGRAS DO FIRESTORE — PsiCorrection
//  Cole estas regras em:
//  Firebase Console → Firestore Database → Regras
// ═══════════════════════════════════════════════════════
//
//  Estratégia de segurança:
//  - Exige autenticação anônima (feita automaticamente pelo app no carregamento)
//  - Impede acesso de scripts externos sem o SDK Firebase
//  - Controle de permissões é feito pela lógica da aplicação
//
rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {

    // Tabelas normativas: leitura autenticada, escrita NUNCA via cliente
    match /_normas/{doc} {
      allow read:  if request.auth != null;
      allow write: if false;
    }

    // Demais coleções: requer autenticação (anônima ou real)
    match /{document=**} {
      allow read, write: if request.auth != null;
    }
  }
}

// ───────────────────────────────────────────────────────
//  CONFIGURAÇÃO ADICIONAL RECOMENDADA:
//
//  1. No Firebase Console → Configurações do projeto → Restrições de API:
//     Restrinja a chave de API para aceitar apenas o domínio do seu site.
//     Ex: https://luangs.pro/*
//
//  2. Nunca exponha chaves de serviço (service account) no frontend.
// ───────────────────────────────────────────────────────
